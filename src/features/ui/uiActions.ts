/**
 * Cross-slice UI actions.
 *
 * Most UI actions are handled directly by the uiSlice reducers.
 * This file exists as a placeholder for any cross-slice UI-related
 * actions that may be needed in later phases (e.g., a combined action
 * that selects a node and opens the config panel simultaneously from
 * a non-UI context).
 *
 * The resetApp action (used to clear UI state) is defined in
 * ../workflow/workflowActions.ts and is handled by the uiSlice's
 * extraReducers.
 */
export {};
